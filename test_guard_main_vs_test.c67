// Compare main vs other variable name
main = {
    | 1 == 1 => 100
    ~> 999
}

test = {
    | 1 == 1 => 200
    ~> 888
}

println(f"main (via exit code): should be 100")
println(f"test = {test} (should be 200)")
